<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.cs553.collaborativestudy.CreatePostFragment">

    <!-- TODO: Update blank fragment layout -->
    <EditText android:id="@+id/courseId"
        android:layout_weight="1"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:hint="Course Id"
         />

    <EditText android:id="@+id/courseDesc"
        android:layout_weight="1"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/courseId"
        android:hint="Course Description" />

    <EditText android:id="@+id/location"
        android:layout_weight="1"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/courseDesc"
        android:hint="Location" />

    <EditText android:id="@+id/time"
        android:layout_weight="1"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/location"
        android:hint="Time and Date" />
    <Button
        android:id="@+id/createPost"
        android:layout_weight="1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Create New Post"
        android:layout_marginTop="50dp"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/time"/>

</RelativeLayout>
